{
  "mcpServers": {
    "connector-hub": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/connector-hub-mcp",
      "args": [
        "mcp"
      ],
      "env": {
        "CONNECTOR_HUB_SPECS_DIR": "${CLAUDE_PLUGIN_ROOT}/specs"
      }
    }
  }
}
